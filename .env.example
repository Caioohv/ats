# Variáveis lidas pelo docker-compose (raiz).
# Copie para .env e ajuste conforme necessário.

# Credenciais do RabbitMQ (usadas pelo broker, backend e bot)
RABBITMQ_USER=bomcurriculo
RABBITMQ_PASS=bomcurriculo
RABBITMQ_VHOST=/

# Portas publicadas no host (apenas produção)
FRONTEND_PORT=5173
BACKEND_PORT=8080
BOT_PORT=8000

# URL da API embutida no build do frontend (produção)
VITE_API_URL_CONNECT=http://127.0.0.1:8080/api
